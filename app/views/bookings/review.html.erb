<%= simple_form_for(@booking, method: :patch, url:"/bookings/#{@booking.id}/update") do |f| %>
  <%= f.input :review_title %>
  <%= f.input :review_content %>
  <%= f.input :review_rating, collection: 0..5 %>
  <%= f.button :submit %>
<% end %>
